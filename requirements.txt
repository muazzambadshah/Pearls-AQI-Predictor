# ==========================================================================
# Pearls AQI Predictor
# Tested on Python 3.11 - 3.12
# ==========================================================================

# ---------- Core data ----------
pandas>=2.2,<2.4
numpy>=1.26,<2.3
pyarrow>=15.0          # parquet feature store
requests>=2.31
python-dotenv>=1.0

# ---------- Machine learning ----------
scikit-learn>=1.4
lightgbm>=4.3
statsmodels>=0.14      # ADF stationarity test, STL decomposition
joblib>=1.3
shap>=0.44             # feature attribution

# PyTorch is REQUIRED, not optional. The current production model is
# `blend_top2` - an average of the PyTorch MLP and LightGBM - and the MLP was
# the best single candidate in the backtest. Unpickling models/best_model.pkl
# therefore imports torch, and an install without it fails at load time with a
# bare ModuleNotFoundError.
#
# The extra index below pulls the CPU-only build (~200MB instead of the ~2.5GB
# CUDA bundle). Nothing here needs a GPU: the MLP trains on CPU in seconds.
#
# If you retrain and a non-neural candidate wins, torch stops being needed to
# *serve* - but keep it installed to let the MLP compete in the next backtest.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2

# TensorFlow, for the tf_mlp_anchored candidate (a Keras MLP, evaluated
# alongside the PyTorch one). Optional: only imported when ENABLE_TF_MODEL=1;
# the training pipeline skips this candidate cleanly if it's absent.
tensorflow-cpu>=2.16; sys_platform != "darwin"
tensorflow>=2.16; sys_platform == "darwin"

# ---------- Dashboard & API ----------
streamlit>=1.32
plotly>=5.18
matplotlib>=3.8        # static figures for the report
fastapi>=0.110
uvicorn[standard]>=0.29
flask>=3.0              # alternate REST API, same routes/JSON as app/api.py

# ---------- Testing ----------
pytest>=8.0
httpx>=0.27            # required by fastapi.testclient

# ---------- Optional ----------
# Managed feature store. The local parquet backend is the default and needs no
# account; set FEATURE_STORE_BACKEND=hopsworks to use this instead.
# hopsworks>=3.7
